import type { ServiceProvider } from './types';

/**
 * Service provider directory.
 * Currently empty — providers will be added as real partnerships are established.
 * All contact details must be verified before publishing.
 */
export const serviceProviders: ServiceProvider[] = [];
